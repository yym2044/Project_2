<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yym.infra.modules.product.ProductMpp">

	<resultMap id="resultMapObj"
		type="com.yym.infra.modules.product.Product"></resultMap>
	
	<select id="selectList">
		
		SELECT
			trpdSeq
			, trpdName
			, trpdPrice
			, trpdDiscountPercent
			, trpdDeliveryFee
			, trpdStarRating
		FROM 
			tradProduct
		WHERE 1=1
			AND trpdDelNy = 0
			AND trpdUseNy = 1
		<choose>
			<when test="shCategory == 0">
				AND (trpdName like concat('%',#{shVar},'%') OR trpdTag like concat('%',#{shVar},'%')
			</when> 
			<otherwise>
				AND (trpdName like concat('%',#{shVar},'%') OR trpdTag like concat('%',#{shVar},'%'))
				AND trpdCategoryCd2 = ${shCategory}
			</otherwise>
				
		</choose>
			
	
	</select>
	

</mapper>